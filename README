Active Bugs:

- Picking doesn't work correctly with ghosts spawned from die roll
- Segfault on exit
- Too much debug printing

Required libraries for Ubuntu:
freeglut3-dev
libglew-dev
libxmu-dev 
libxi-dev
